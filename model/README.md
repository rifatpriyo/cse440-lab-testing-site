# Model file

Place the real exported `LR_C1.joblib` file in this folder:

```text
web/model/LR_C1.joblib
```

It must be the fitted Logistic Regression + TF-IDF pipeline created by the project notebook. Do not place a newly trained or placeholder model here.
